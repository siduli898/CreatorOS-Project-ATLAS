const {generateUUID}=require('../../utils/crypto');const {recordAudit}=require('../../utils/audit');
class ModerationService{constructor(){this.cases=new Map();}fileReport({reporterUserId,targetEntityType,targetEntityId,reasonCode,details=''}){const id=generateUUID();let severity='LOW';if(reasonCode==='CSAM_SUSPICION')severity='CRITICAL_LEGAL';else if(reasonCode==='SOLICITATION_PHYSICAL_SERVICES'||reasonCode==='NON_CONSENSUAL')severity='HIGH';const c={id,reporterUserId,targetEntityType,targetEntityId,reasonCode,severity,status:'QUEUED',actionTaken:'NONE',details,createdAt:new Date().toISOString()};this.cases.set(id,c);recordAudit({actorUserId:reporterUserId,actorRole:'USER',action:'FILE_SAFETY_REPORT',resourceType:targetEntityType,resourceId:targetEntityId,details:{reasonCode,severity}});return c;}getQueuedCases(){return [...this.cases.values()];}}
module.exports=new ModerationService();
